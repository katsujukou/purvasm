# Dune build artifacts
_build/
*.install
*.merlin

# Local opam switch / lockfiles
_opam/
*.opam.locked

# Coverage / docs output
_coverage/
bisect*.coverage
_doc/

# OCaml compiler outputs (in case anything is built outside dune)
*.cmi
*.cmo
*.cmx
*.cmt
*.cmti
*.cma
*.cmxa
*.cmxs
*.o
*.a
*.so
*.byte
*.native

# Editor / utop
.*.swp
*~

# NOTE: purvasm.opam is generated by `generate_opam_files`, but the dune
# convention is to commit it, so it is intentionally NOT ignored here.
