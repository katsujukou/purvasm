package:
  name: purvasm-regex
  dependencies:
    - arrays
    - either
    - foldable-traversable
    - lists
    - maybe
    - partial
    - prelude
    - purvasm-base
  test:
    main: Test.Unit.Regex.Core
    dependencies:
      - effect
      - spec
      - spec-node
